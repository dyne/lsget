services:
  lsget:
    build: .
    container_name: lsget
    ports:
      - "4000:4000"
    volumes:
      - ./files:/data
    restart: unless-stopped
    command: ["-dir", "/data", "-addr", "0.0.0.0:4000"]
